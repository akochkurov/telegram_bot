
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	А = 4;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ЭтаФорма.Окно <> Неопределено
		И ЭтаФорма.Окно.Содержимое.Количество() > 0
		И ЭтаФорма.Окно.Содержимое[0].ИмяФормы = "Справочник.ТелеграмБоты.Форма.ФормаЭлемента" Тогда
 		Элементы.Бот.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры
