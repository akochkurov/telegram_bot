
&НаКлиенте
Процедура Перенести(Команда)
	
	Ответ = Новый Структура;
	Ответ.Вставить("ПараметрПринадлежит", ПараметрПринадлежит);
	Ответ.Вставить("Параметр", Параметр);
	Ответ.Вставить("ЗначениеПараметра", ЗначениеПараметра);
	ЭтаФорма.Закрыть(Ответ);
	
КонецПроцедуры
