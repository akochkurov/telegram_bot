&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЭтаФорма.Заголовок = Параметры.Заголовок;
	Сообщение = Параметры.Сообщение;
	
	ХТМЛ = Ложь;
	Если Параметры.Свойство("ХТМЛ") Тогда
		ХТМЛ = Параметры.ХТМЛ;
	КонецЕсли;
	
	Если НЕ ХТМЛ Тогда
		Элементы.Страницы.ТекущаяСтраница = Элементы.СтраницаТекст
	Иначе
		Элементы.Страницы.ТекущаяСтраница = Элементы.СтраницаХТМЛ;
	КонецЕсли;
	
КонецПроцедуры